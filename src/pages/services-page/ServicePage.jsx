import ServicesPageCreator from "../../components/services-page-creator/ServicesPageCreator";

const ServicePage = ({ url }) => {
  return <ServicesPageCreator url={url} />;
};

export default ServicePage;
